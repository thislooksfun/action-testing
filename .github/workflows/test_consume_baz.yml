name: Consume Event (baz)

on:
  repository_dispatch:
    types:
      tlf-custom-event:
        foo: baz

jobs:
  dispatch:
    runs-on: ubuntu-latest
    steps:
      - name: It worked!
        run: echo 'yay!'
